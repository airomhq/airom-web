# Vercel CLI link state — per-machine, never shared.
.vercel

# Documentation build output. `mint export` writes a zip; docs-site/build.sh
# unpacks it into dist/ (~50 MB). Both are regenerated by every build.
/docs-site/export.zip
/docs-site/dist/
/docs-site/.mintlify/
node_modules/

.DS_Store
